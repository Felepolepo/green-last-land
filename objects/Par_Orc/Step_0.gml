Script_menu_Pause(1); // ou outro valor padrão de velocidade
if (global.pausado) exit; // Ou: return;

script_execute(estado);
depth = -y;

//Definir nos Obj_Orcs 
/* 

if vida <= 0 {
    obj_personagem.xp += 15;
    instance_destroy();
}


*/